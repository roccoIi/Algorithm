-- 코드를 입력하세요

SELECT B.ID, B.NAME, A.HOST_ID
FROM (
    SELECT HOST_ID
    FROM PLACES
    GROUP BY HOST_ID
        HAVING COUNT(ID) >= 2
    ) A
LEFT JOIN PLACES B ON A.HOST_ID = B.HOST_ID
ORDER BY ID;



